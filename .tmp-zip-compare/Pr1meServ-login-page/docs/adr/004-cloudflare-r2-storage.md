# ADR 004: Cloudflare R2 Storage

Cloudflare R2 is used for object storage because it supports S3-compatible tooling and fits document and import storage needs without adding app-specific storage abstractions.
